networkId: testnet04
publicMeta:
  chainId: "{{{chain}}}"
  gasLimit: 100000
  ttl: 7200
  sender: "does not matter"
  gasPrice: 0.000001
code: |+
      (map (describe-module) (filter (lambda (x) (contains "{{{namespace}}}." x)) (list-modules)))
